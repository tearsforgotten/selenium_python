#/usr/local/ev python

from selenium import webdriver
driver=webdriver.Firefox()

ele=WebDriverWait(driver,30).until(lambda driver:driver_find_elemet_id=（"kw"）)
ele.send_keys("baidu").click


ele=WebDriverWait(driver,30).until(lambda driver:driver_find_element_id=("kw"))
